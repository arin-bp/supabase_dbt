target/
dbt_packages/
logs/
.env
# dbt Cloud local user ID — not project-specific, should not be shared
.user.yml
# Note: profiles.yml is intentionally tracked in git.
# It contains NO credentials — auth is handled by Snowflake's native
# DBT Project object execution context (role-based).
