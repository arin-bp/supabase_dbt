version: 2

sources:
  - name: supabase
    schema: "{{ env_var('SUPABASE_DB_SCHEMA', 'public') }}"
    tables:
      - name: customers
        description: "Raw customer details."
      - name : orders
        description : "Raw ord details"
      - name : products
        description : "raw product details"
